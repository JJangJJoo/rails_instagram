<div class ="container">
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
    <a class="navbar-brand" href="/">게시판</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <% if session[:user_id] %>
          <span class="navbar-text"><%  %></span>
          <a class="nav-item nav-link" href="/logout">로그아웃</a>
        <% else %>
          <a class="nav-item nav-link" href="/login">로그인 <span class="sr-only">(current)</span></a>
        <% end %>
          <a class="nav-item nav-link" href="/signup">회원가입</a>
          <a class="nav-item nav-link" href="/admin">관리자</a>
      </div>
    </div>
  </nav>
  <% if flash[:notice]%>
    <div class="alert alert-primary" role="alert"><%= flash[:notice] %></div>
  <% elsif flash[:alert]%>
    <div class="alert alert-danger" role="alert"><%= flash[:alert] %></div>
  <% end %>
</div>
